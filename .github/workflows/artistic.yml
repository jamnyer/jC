name: blank

on:
  push:

jobs:
  before:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        bare: ["realistic", "faulty", "donkey", "feeling", "encourage"]
        harden: ["outstanding", "dial", "locate", "hear"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar LL
